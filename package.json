{
  "name": "ttg-package-tracking",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf:bootstrap": "node scripts/cloudflare-bootstrap.mjs",
    "cf:deploy": "wrangler deploy --config wrangler.generated.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

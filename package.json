{
  "name": "ttg-package-tracking",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf:bootstrap": "node --check src/router.js && node --check src/ops-auth.js && node --check src/ops-api.js && node --check src/d1-bootstrap.js && node --check src/transaction-reserve.js && node --no-warnings scripts/check-transaction-reserve.mjs",
    "cf:deploy": "wrangler deploy",
    "cf:version": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

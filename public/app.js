(()=>{
const $=id=>document.getElementById(id);
const STAGES=['intake_received','disclaimer_confirmed','deposit_received','parts_sourcing','parts_ordered','awaiting_seller_shipment','seller_shipped','shipping_company_received','awaiting_shipping_cost','shipping_cost_paid','in_transit_to_zambia','received_in_zambia','parts_received_by_ttg','repair_in_progress','testing','ready_for_collection','completed'];
const LABEL={intake_received:'Intake received',disclaimer_confirmed:'Disclaimer confirmed',deposit_received:'Order payment received',parts_sourcing:'Parts sourcing',parts_ordered:'Parts ordered',awaiting_seller_shipment:'Awaiting seller shipment',seller_shipped:'Seller shipped',shipping_company_received:'Shipping company received',awaiting_shipping_cost:'Awaiting shipping cost',shipping_cost_paid:'Shipping cost paid',in_transit_to_zambia:'In transit to Zambia',received_in_zambia:'Received in Zambia / local shipping received',parts_received_by_ttg:'Parts received by TTG',repair_in_progress:'Repair in progress',testing:'Testing',ready_for_collection:'Ready for collection',completed:'Completed'};
const demo={found:true,reference:'TTG-RCP-000060',masterId:'TTG-TXN-000060',clientName:'Example Client',condition:'Device / order under tracking',deviceItem:'Tracked item',serviceType:'Parts / Order Tracking',route:'USA → Zambia',origin:'USA',location:'USA shipping company',amountReceived:'',paymentMethod:'Mobile Money',orderPaymentStatus:'Paid in full',shippingCostStatus:'Awaiting shipping company quote',stage:'awaiting_shipping_cost',updatedLabel:'08 Aug 2026',statusNote:'The order/item is paid in full. The parcel has reached the USA shipping company and TTG is waiting for the international shipping charge to be confirmed before dispatch toward Zambia.',latestUpdate:'Shipping company received the parcel. International shipping cost is pending confirmation.',events:[{label:'Payment received in full',location:'Zambia',createdAt:'2026-08-06T13:20:00+02:00'},{label:'Order created',location:'US',createdAt:'2026-08-07T14:04:00-05:00'},{label:'In FedEx possession — package received after final location pickup',location:'Manhattan, KS, US',createdAt:'2026-08-07T15:51:00-05:00'},{label:'Picked up',location:'Topeka, KS, US',createdAt:'2026-08-07T16:45:00-05:00'},{label:'Arrived at FedEx location',location:'Topeka, KS, US',createdAt:'2026-08-07T21:58:00-05:00'}]};
const wait=ms=>new Promise(r=>setTimeout(r,ms));
const norm=v=>String(v||'').trim().toUpperCase();
const text=(id,v)=>{const el=$(id);if(el)el.textContent=(v===undefined||v===null||v==='')?'—':v};
const money=v=>typeof v==='number'?`ZMW ${v.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}`:v;
function updateStages(stage){const ix=Math.max(0,STAGES.indexOf(stage));document.querySelectorAll('.step[data-stage]').forEach((el,i)=>{el.classList.toggle('done',i<ix);el.classList.toggle('current',i===ix);const s=el.querySelector('small');if(s)s.textContent=i<ix?'Completed':i===ix?'In progress':'Pending'});requestAnimationFrame(centerStage)}
function centerStage(){if(innerWidth>900)return;const s=$('steps'),c=s?.querySelector('.current');if(s&&c)s.scrollLeft=Math.max(0,c.offsetLeft-(s.clientWidth-c.clientWidth)/2)}
function jumpToProgress(){const el=$('jobProgress');if(!el)return;const go=()=>{const h=document.querySelector('.topbar')?.getBoundingClientRect().height||0;const top=el.getBoundingClientRect().top+scrollY-h-12;scrollTo({top:Math.max(0,top),behavior:'smooth'})};requestAnimationFrame(()=>requestAnimationFrame(go));setTimeout(go,300)}
function apply(r0,id){const r={...demo,...r0},ref=r.reference||id;$('resultArea').hidden=false;text('resultRef',ref);text('publicReference',ref);text('summaryReference',ref);text('clientName',r.clientName);text('condition',r.condition);text('deviceItem',r.deviceItem);text('serviceType',r.serviceType);text('masterId',r.masterId);text('summaryMaster',r.masterId);text('route',r.route);text('summaryRoute',r.route);text('amountReceived',money(r.amountReceived));text('paymentMethod',r.paymentMethod);text('orderPaymentStatus',r.orderPaymentStatus||'Paid / confirmed');text('shippingCostStatus',r.shippingCostStatus||'Not yet confirmed');text('summaryPayment',r.orderPaymentStatus||'Paid / confirmed');text('summaryShippingCost',r.shippingCostStatus||'Not yet confirmed');const label=LABEL[r.stage]||r.status||'Tracking update';text('currentStatus',label);text('summaryStatus',label);text('statusNote',r.statusNote);text('summaryLocation',r.location||r.origin);text('summaryUpdated',r.updatedLabel);text('lastUpdated',`Last updated: ${r.updatedLabel} ↻`);const latest=$('latestUpdate');if(latest)latest.innerHTML=`<b>${r.updatedLabel}${r.location||r.origin?` · ${r.location||r.origin}`:''}</b><br>${r.latestUpdate||r.statusNote||''}`;updateStages(r.stage||'intake_received')}
async function lookup(id){try{const res=await fetch(`/api/track?id=${encodeURIComponent(id)}`,{headers:{accept:'application/json'}});return res.ok?await res.json():{found:false}}catch{return /^TTG-(RCP|INV|DOC|QTE|TXN)-0*60$/i.test(id)?demo:{found:false}}}
const form=$('searchForm'),input=$('trackingInput'),badge=$('foundBadge'),overlay=$('trackingOverlay'),ovH=overlay?.querySelector('h2'),ovP=overlay?.querySelector('p');
form?.addEventListener('submit',async e=>{e.preventDefault();const id=norm(input?.value);if(!id)return;if(ovH)ovH.textContent='Tracking your TTG job…';if(ovP)ovP.textContent='Checking the latest saved progress and shipping handoff.';overlay?.classList.add('show');overlay?.setAttribute('aria-hidden','false');const started=Date.now(),r=await lookup(id);await wait(Math.max(0,3000-(Date.now()-started)));if(r?.found!==false){overlay?.classList.remove('show');overlay?.setAttribute('aria-hidden','true');badge.textContent='Found';badge.classList.remove('bad');apply(r,id);await wait(120);jumpToProgress()}else{if(ovH)ovH.textContent='Tracking ID not found';if(ovP)ovP.textContent='Check the TTG document number and try again.';await wait(1200);overlay?.classList.remove('show');overlay?.setAttribute('aria-hidden','true')}});
requestAnimationFrame(centerStage);addEventListener('resize',centerStage,{passive:true});
const panel=$('tgai-panel'),fab=$('tgai-fab'),close=$('tgai-close'),card=$('mayaCardOpen'),mform=$('tgaiForm'),minput=$('tgaiInput'),msgs=$('tgaiMessages');let frozen=0;
function viewport(){const v=visualViewport,h=v?.height||innerHeight,t=v?.offsetTop||0;document.documentElement.style.setProperty('--vv-height',`${Math.round(h)}px`);document.documentElement.style.setProperty('--vv-top',`${Math.round(t)}px`)}
function lock(){if(document.body.classList.contains('tgai-open'))return;frozen=scrollY;document.body.style.top=`-${frozen}px`;document.body.classList.add('tgai-open');viewport()}
function unlock(){if(!document.body.classList.contains('tgai-open'))return;document.body.classList.remove('tgai-open');document.body.style.top='';scrollTo(0,frozen)}
function openMaya(){lock();panel?.classList.add('open');panel?.setAttribute('aria-hidden','false');viewport()}
function closeMaya(){minput?.blur();panel?.classList.remove('open');panel?.setAttribute('aria-hidden','true');unlock()}
fab?.addEventListener('click',openMaya);card?.addEventListener('click',openMaya);close?.addEventListener('click',closeMaya);visualViewport?.addEventListener('resize',viewport,{passive:true});visualViewport?.addEventListener('scroll',viewport,{passive:true});addEventListener('orientationchange',()=>setTimeout(viewport,120),{passive:true});
function addMsg(s,user=false){const d=document.createElement('div');d.className=`tgai-msg${user?' user':''}`;d.textContent=s;msgs?.appendChild(d);if(msgs)msgs.scrollTop=msgs.scrollHeight}
function typing(){const d=document.createElement('div');d.className='tgai-typing';d.id='tgaiTyping';d.textContent='Maya is typing…';msgs?.appendChild(d);if(msgs)msgs.scrollTop=msgs.scrollHeight}
function fmtDate(v){const d=new Date(v);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Africa/Lusaka'}).format(d).replace(',','')}
function demoTimeline(){return demo.events.map(e=>`${fmtDate(e.createdAt)} — ${e.label}${e.location?` · ${e.location}`:''}`).join('\n')}
function fallback(q0){const q=q0.toLowerCase();if(/hello|hey|^hi\b/.test(q))return'Hi 👋🏽 I’m here. Ask me about this tracked job, its dated history, what is pending, or what happens next.';if(/\bdetail(s)?\b|\babout this\b|\btell me about\b|\bsummary\b|\binformation\b/.test(q))return`This TTG job is a USA → Zambia parts/order shipment. The item itself is paid in full. The parcel has reached the assigned USA shipping company, and TTG is waiting for the international shipping charge to be confirmed.\n\nDated history:\n${demoTimeline()}\n\nAfter the shipping charge is confirmed and paid, the next stage is dispatch toward Zambia. USA transit is normally about 21 working days after that international handoff.`;if(/\bdate(s)?\b|\btimeline\b|\bhistory\b|\bwhen\b/.test(q))return`Here’s the dated tracking history:\n${demoTimeline()}`;if(/\bnext\b|what happens/.test(q))return'The order itself is paid in full. TTG is waiting for the shipping company to confirm the international shipping price. Once that shipping cost is paid, the parcel moves into the Zambia transit stage.';if(/japan/.test(q))return'For genuine parts from Japan, the normal guide is about 14 working days after the shipping handoff.';if(/china/.test(q)&&/large|heavy|big|bulky|freight|sea/.test(q))return'Large or heavy items from China are normally about 60–70 days. Small China parcels are usually about 7–14 working days.';if(/china/.test(q))return'Small China parcels are usually about 7–14 working days; large or heavy items are normally about 60–70 days.';if(/\buk\b|britain|england/.test(q))return'UK shipments are normally about 14 working days or less after shipping handoff.';if(/\busa\b|america|eta|how long|delivery|arrive/.test(q))return'USA shipments are normally about 21 working days after the international shipping handoff. Seller processing before that is separate.';return'I can explain this job’s dated tracking history, current stage, payment and shipping-cost status, what happens next, delivery timing, customs, or linked TTG documents.'}
async function askMaya(message){try{const res=await fetch('/api/maya',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message,trackingId:norm(input?.value)})});if(res.ok){const d=await res.json();if(d?.reply)return d.reply}}catch{}return fallback(message)}
mform?.addEventListener('submit',async e=>{e.preventDefault();const s=minput?.value.trim();if(!s)return;addMsg(s,true);minput.value='';typing();const reply=await askMaya(s);await wait(240);$('tgaiTyping')?.remove();addMsg(reply)});
})();

(()=>{
const ORDER=['intake_received','disclaimer_confirmed','deposit_received','parts_sourcing','parts_ordered','awaiting_seller_shipment','seller_shipped','shipping_company_received','in_transit_to_zambia','received_in_zambia','awaiting_shipping_cost','shipping_cost_paid','parts_received_by_ttg','repair_in_progress','testing','ready_for_collection','completed'];
const LABEL={intake_received:'Intake<br>received',disclaimer_confirmed:'Disclaimer<br>confirmed',deposit_received:'Order payment<br>received',parts_sourcing:'Parts<br>sourcing',parts_ordered:'Parts<br>ordered',awaiting_seller_shipment:'Awaiting seller<br>shipment',seller_shipped:'Seller<br>shipped',shipping_company_received:'Shipping company<br>received',in_transit_to_zambia:'In transit to<br>Zambia',received_in_zambia:'Zambia local<br>pickup center',awaiting_shipping_cost:'Awaiting shipping<br>cost',shipping_cost_paid:'Shipping cost<br>paid',parts_received_by_ttg:'Parts received<br>by TTG',repair_in_progress:'Repair in<br>progress',testing:'Testing',ready_for_collection:'Ready for<br>collection',completed:'Completed'};
const $=id=>document.getElementById(id),NOT_DUE='Not yet due — confirmed at Zambia local pickup center',LOCAL='Pending confirmation at Zambia local pickup center';
let fixing=false;
function setText(el,value){if(el&&el.textContent!==value)el.textContent=value}
function setHtml(el,value){if(el&&el.innerHTML!==value)el.innerHTML=value}
function inZambia(){return /(zambia|lusaka|kitwe|ndola|livingstone|\bzm\b)/i.test($('summaryLocation')?.textContent||'')}
function steps(){return[...document.querySelectorAll('#steps .step[data-stage]')]}
function visualStage(){let current=document.querySelector('#steps .step.current')?.dataset.stage||'';if(current==='awaiting_shipping_cost'&&!inZambia())current='shipping_company_received';return current}
function relabel(){steps().forEach(el=>{const st=el.dataset.stage,ix=ORDER.indexOf(st);if(ix<0)return;const order=String(ix);if(el.style.order!==order)el.style.order=order;const i=el.querySelector('i');if(i&&st!=='completed')setText(i,String(ix+1));setHtml(el.querySelector('span'),LABEL[st])})}
function repair(){if(fixing)return;fixing=true;relabel();const current=visualStage(),ix=ORDER.indexOf(current);if(ix>=0){steps().forEach(el=>{const p=ORDER.indexOf(el.dataset.stage),small=el.querySelector('small'),state=p<ix?'Completed':p===ix?'In progress':'Pending';el.classList.toggle('done',p<ix);el.classList.toggle('current',p===ix);setText(small,state)});const cost=$('shippingCostStatus'),sumCost=$('summaryShippingCost');if(current==='shipping_company_received'||current==='in_transit_to_zambia'){setText(cost,NOT_DUE);setText(sumCost,NOT_DUE)}else if(current==='received_in_zambia'){if(cost&&(cost.textContent==='—'||/not yet due|awaiting shipping company quote/i.test(cost.textContent)))setText(cost,LOCAL);if(sumCost&&(sumCost.textContent==='—'||/not yet due|awaiting shipping company quote/i.test(sumCost.textContent)))setText(sumCost,LOCAL)}if(current==='shipping_company_received'){setText($('currentStatus'),'Shipping company received');setText($('summaryStatus'),'Shipping company received');const note=$('statusNote');if(note&&/shipping (cost|charge|price)|waiting.*shipping/i.test(note.textContent))setText(note,'The parcel has reached the assigned origin shipping company. The next logistics stage is international transit toward Zambia. Shipping cost is normally confirmed later, after the parcel reaches the Zambia local pickup center.')}const wrap=$('steps'),cur=wrap?.querySelector('.current');if(wrap&&cur&&innerWidth<=900)requestAnimationFrame(()=>{const left=Math.max(0,cur.offsetLeft-(wrap.clientWidth-cur.clientWidth)/2);if(Math.abs(wrap.scrollLeft-left)>2)wrap.scrollLeft=left})}fixing=false}
const target=$('steps');if(target){new MutationObserver(()=>requestAnimationFrame(repair)).observe(target,{subtree:true,attributes:true,attributeFilter:['class'],childList:true,characterData:true});repair()}
const result=document.getElementById('resultArea');if(result)new MutationObserver(()=>requestAnimationFrame(repair)).observe(result,{subtree:true,childList:true,characterData:true});
addEventListener('resize',()=>requestAnimationFrame(repair),{passive:true});
})();
